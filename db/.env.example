# Vieques AI — database environment
# Copy this file to  db/.env  and fill in a real password.
# db/.env is gitignored; never commit real credentials.

DB_NAME=vieques_ai
DB_USER=vieques_app
DB_PASSWORD=vieques1
DB_HOST=localhost
DB_PORT=5432

# Full connection string (handy for app code / psql):
# DATABASE_URL=postgresql://vieques_app:change_me@localhost:5432/vieques_ai
